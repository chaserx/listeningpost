json.extract! webhook, :id, :message_body, :device_id, :url, :headers,
                       :access_token, :request_type, :payload, :query, :auth,
                       :user_id, :created_at, :updated_at
